"""
============================================================
📌 Python 수학(Math) — 내장 함수 + math 모듈
============================================================

Python은 기본적인 수학 함수와 더불어, 
추가적인 기능을 제공하는 math 모듈도 포함함.
"""

"""
============================================================
📌 1. 내장(Built-in) 수학 함수
============================================================

● min()  → 가장 작은 값
● max()  → 가장 큰 값
예:
    x = min(5, 10, 25)   # 5
    y = max(5, 10, 25)   # 25
    print(x, y)

● abs()  → 절대값(항상 양수로 반환)
예:
    x = abs(-7.25)       # 7.25

● pow(x, y) → x의 y 제곱
예:
    pow(4, 3)  # 64  (4×4×4)

※ pow()는 내장 함수이며, 연산자 ** 와 동일:
    4 ** 3  # 64
"""
# 파이썬 내장 수학 함수

x = min(5, 10, 25)
y = max(5, 10, 25)
print(x, y)
# 결과 : 5 25

x = abs(-7.25)
print(x)
# 결과 : 7.25

z = pow(4, 3)
print(z)
# 결과 : 64

"""
============================================================
📌 2. math 모듈 — 확장된 수학 기능
============================================================

math 모듈 사용:
    import math

가져온 후 math.함수 형태로 사용.
"""

"""
============================================================
📌 3. 대표적인 math 모듈 함수
============================================================

● 제곱근 — math.sqrt()
예:
    math.sqrt(64)   # 8.0

● 올림 — math.ceil()
예:
    math.ceil(1.4)  # 2

● 내림 — math.floor()
예:
    math.floor(1.4) # 1

● 수학 상수 PI — math.pi
예:
    math.pi   # 3.141592...
"""
# 파이썬 math 모듈 주요 함수 예제

import math
x = math.sqrt(64)
print(x)  # 8.0
y = math.ceil(1.4)
print(y)  # 2
z = math.floor(1.4)
print(z)  # 1
p = math.pi
print(p)  # 3.141592653589793

"""
============================================================
📌 math 모듈 — 자주 사용되는 기타 기능 요약
============================================================

● math.factorial(n)  
  n! (팩토리얼) 계산

● math.pow(x, y)  
  pow()와 비슷하지만 float 반환

● math.sin(), math.cos(), math.tan()  
  삼각함수

● math.degrees(), math.radians()  
  각도 ↔ 라디안 변환

● math.log(x)        → 자연로그
● math.log10(x)      → 밑 10 로그
● math.exp(x)        → e^x

● math.isnan(x), math.isinf(x)  
  NaN / 무한대 판별

● math.gcd(a, b)  
  최대공약수
"""
# 파이썬 math 모듈 기타 기능 예제

import math
f = math.factorial(5)
print(f)  # 120
fp = math.pow(4, 3)
print(fp)  # 64.0
s = math.sin(math.radians(90))
print(s)  # 1.0
c = math.cos(math.radians(0))
print(c)  # 1.0
t = math.tan(math.radians(45))
print(t)  # 0.9999999999999999
l = math.log(2.718281828459045)
print(l)  # 1.0
l10 = math.log10(1000)
print(l10)  # 3.0
e = math.exp(2)
print(e)  # 7.38905609893065
g = math.gcd(48, 180)
print(g)  # 12

"""
============================================================
📌 핵심 요약
============================================================

⚡ 기본 제공 함수:
- min(), max(), abs(), pow()

⚡ math 모듈 필요:
- 제곱근(sqrt), 올림/내림(ceil, floor)
- 삼각함수, 로그, 상수(pi)

============================================================
"""
