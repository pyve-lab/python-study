"""
============================================================
📌 Python의 None — "값 없음", "비어 있음"을 의미하는 특수 값
============================================================

● None은 Python에서 “값이 없음”을 나타내는 유일한 객체
● 데이터 타입은 NoneType (유일한 인스턴스: None 자체)
● 0, False, 빈 문자열과는 다름 → 오직 "값 없음"을 의미

============================================================
📌 1. None 기본 사용
============================================================

값 할당:
    x = None
출력:
    print(x)          # None
유형 확인:
    type(x)           # <class 'NoneType'>


============================================================
📌 2. None 비교 — 반드시 is / is not 사용!
============================================================

잘못된 비교 ❌:
    result == None  # 권장 X

올바른 비교 ⭕:
    result is None
    result is not None

예:
    if result is None:
        print("No result yet")
    else:
        print("Result is ready")


============================================================
📌 3. Truthy / Falsy 동작
============================================================

None은 불리언 문맥에서 False로 취급됨.

예:
    bool(None)   → False


============================================================
📌 4. 함수에서 None 반환
============================================================

return 문이 없는 함수는 자동으로 None을 반환함.

예:
    def myfunc():
        x = 5   # return 없음!

    result = myfunc()
    print(result)    # None


============================================================
📌 핵심 정리
============================================================

✔ None은 "값 없음"을 의미하는 하나뿐인 객체  
✔ 항상 is / is not 으로 비교해야 함  
✔ bool(None) → False  
✔ return 없는 함수도 자동으로 None 반환  

============================================================
"""
