"""
============================================================
📌 Python 함수(Function) 완전 정리
============================================================

함수란?
- 호출될 때만 실행되는 코드 블록
- 코드 재사용성 증가
- return 으로 값을 반환할 수 있음
"""

"""
============================================================
📌 1. 함수 정의와 호출
============================================================

함수 정의:
    def my_function():
        print("Hello")

함수 호출:
    my_function()


📌 패스(pass)
함수 몸체가 비어 있으면 오류 → pass 사용
    def f():
        pass
"""
# 파이썬 함수 정의와 호출

def my_function():
    print("Hello")
my_function()
# 결과 : Hello

def f():
    pass
# 결과 : (아무 출력 없음)

"""
============================================================
📌 2. 함수 이름 규칙
============================================================
- 문자/밑줄로 시작
- 문자, 숫자, _ 사용 가능
- 대소문자 구분
- 기능을 잘 표현하는 이름 사용 권장
"""

"""
============================================================
📌 3. 함수의 장점
============================================================
- 반복되는 코드를 줄임
- 유지보수 용이
- 구조적 프로그래밍 가능
"""

"""
============================================================
📌 4. return 문
============================================================
함수에서 값을 반환하고 실행 종료.

예:
    def get_msg():
        return "Hello"
    print(get_msg())

return 없는 경우 → None 반환
"""
# 파이썬 return 문

def get_msg():
    return "Hello"
print(get_msg())
# 결과 : Hello

def no_return():
    pass
print(no_return())
# 결과 : None

"""
============================================================
📌 5. 매개변수와 인수
============================================================

매개변수(parameter)
- 함수 정의 시 괄호 안에 있는 변수

인수(argument)
- 함수 호출 시 실제 전달되는 값

예:
    def f(name):   # parameter
        print(name)

    f("Emil")      # argument
"""
# 파이썬 매개변수와 인수

def f(name):   # parameter
    print(name)

f("Emil")      # argument
# 결과 : Emil

"""
============================================================
📌 6. 인수 개수 (정확히 맞춰야 함)
============================================================
기본:
    def f(a, b): ...
    f(1, 2) → OK
    f(1) → 오류
"""

"""
============================================================
📌 7. 기본값(Default Parameter)
============================================================
    def f(name="friend"):
        print("Hello", name)

    f() → 기본값 사용
"""
# 파이썬 기본값 매개변수

def f(name="friend"):
    print("Hello", name)
f()  # 기본값 사용
# 결과 : Hello friend

"""
============================================================
📌 8. 키워드 인수 (keyword argument)
============================================================
키=값 형태로 전달 가능 → 순서 영향 없음

    def f(animal, name):
        print(animal, name)

    f(name="Buddy", animal="dog")
"""
# 파이썬 키워드 인수

def f(animal, name):
    print(animal, name)
f(name="Buddy", animal="dog")
# 결과 : dog Buddy

"""
============================================================
📌 9. 위치 인수(Positional Argument)
============================================================
순서대로 전달해야 함

    f("dog", "Buddy")
"""
# 파이썬 위치 인수

def f(animal, name):
    print(animal, name)
f("dog", "Buddy")
# 결과 : dog Buddy

"""
============================================================
📌 10. 위치 + 키워드 혼합
============================================================
위치 인수가 먼저 와야 함

    f("dog", name="Buddy")
"""
# 파이썬 위치 + 키워드 혼합 인수
def f(animal, name):
    print(animal, name)
f("dog", name="Buddy")
# 결과 : dog Buddy

"""
============================================================
📌 11. 리스트 / 사전 인수 전달
============================================================
목록 전달:
    def f(items):
        for i in items:
            print(i)

사전 전달:
    def f(person):
        print(person["name"])
"""
# 파이썬 리스트 / 사전 인수 전달

def f(items):
    for i in items:
        print(i)
f(["apple", "banana", "cherry"])

# 결과 : apple banana cherry
def f(person):
    print(person["name"])
f({"name": "Alice", "age": 30})
# 결과 : Alice

"""
============================================================
📌 12. 다양한 반환 값
============================================================
리스트 반환:
    return [1,2,3]

튜플 반환:
    return (10, 20)

언패킹 가능:
    a, b = func()
"""
# 파이썬 다양한 반환 값

def return_list():
    return [1, 2, 3]
print(return_list())
# 결과 : [1, 2, 3]

def return_tuple():
    return (10, 20)
a, b = return_tuple()
print(a, b)
# 결과 : 10 20

"""
============================================================
📌 13. 위치 전용 인수 / 키워드 전용 인수
============================================================

● 위치 전용 인수 : / 앞에 있는 인수
    def f(name, /):
        print(name)

→ f(name="Emil") 불가

● 키워드 전용 인수 : * 뒤의 인수
    def f(*, name):
        print(name)

→ f("Emil") 불가 → f(name="Emil")
"""
# 파이썬 위치 전용 인수 / 키워드 전용 인수

def pos_only(name, /):
    print(name)
pos_only("Emil")  # 가능
# 결과 : Emil

def kw_only(*, name):
    print(name)
kw_only(name="Emil")  # 가능
# 결과 : Emil

"""
============================================================
📌 14. *args (임의의 개수의 위치 인수)
============================================================
    def f(*args):
        print(args)

→ 여러 개 전달 가능, 내부에서는 튜플

예:
    f(1,2,3)
"""
# 파이썬 *args

def f(*args):
    print(args)
f(1, 2, 3)
# 결과 : (1, 2, 3)

"""
============================================================
📌 15. **kwargs (임의의 키워드 인수)
============================================================
    def f(**kwargs):
        print(kwargs)

→ 내부에서는 dict
"""
# 파이썬 **kwargs

def f(**kwargs):
    print(kwargs)
f(name="Alice", age=30)
# 결과 : {'name': 'Alice', 'age': 30}

"""
============================================================
📌 16. *args + **kwargs 조합
============================================================
순서:
1) 일반 매개변수
2) *args
3) **kwargs

    def f(title, *args, **kwargs):
        ...
"""
# 파이썬 *args + **kwargs 조합

def f(title, *args, **kwargs):
    print("Title:", title)
    print("Args:", args)
    print("Kwargs:", kwargs)
f("My Function", 1, 2, name="Alice", age=30)
# 결과 :
# Title: My Function
# Args: (1, 2)
# Kwargs: {'name': 'Alice', 'age': 30}

"""
============================================================
📌 17. 인수 풀기(Unpacking)
============================================================

리스트 → 위치 인수
    f(*[1,2,3])

사전 → 키워드 인수
    f(**{"a":1, "b":2})
"""
# 파이썬 인수 풀기(Unpacking)

def f(a, b, c):
    print(a, b, c)
f(*[1, 2, 3])  # 리스트 언패킹
# 결과 : 1 2 3

f(**{"a": 1, "b": 2, "c": 3})  # 사전 언패킹
# 결과 : 1 2 3

"""
============================================================
📌 18. 변수 범위 (Scope)
============================================================

● Local (함수 내부)
● Enclosing (중첩된 함수 바깥)
● Global (모듈 전체)
● Built-in (파이썬 내장)

LEGB 순서로 탐색
"""

"""
============================================================
📌 19. global 키워드
============================================================
로컬에서 글로벌 변수 수정

    x = 10
    def f():
        global x
        x = 20
"""
# 파이썬 global 키워드

x = 10
def f():
    global x
    x = 20
f()
print(x)  # 20

"""
============================================================
📌 20. nonlocal 키워드
============================================================
중첩된 함수에서 바깥 함수 변수 수정

    def outer():
        x = 10
        def inner():
            nonlocal x
            x = 20
"""
# 파이썬 nonlocal 키워드

def outer():
    x = 10
    def inner():
        nonlocal x
        x = 20
    inner()
    print(x)
outer()  # 20

"""
============================================================
📌 21. 데코레이터 (Decorator)
============================================================

● 기존 함수 코드를 수정하지 않고 기능 추가
● @decorator_name 으로 적용

기본 형태:
    def deco(func):
        def wrapper():
            return func().upper()
        return wrapper

    @deco
    def hi():
        return "hello"
"""
# 파이썬 데코레이터 기본 형태

def deco(func):
    def wrapper():
        return func().upper()
    return wrapper

@deco
def hi():
    return "hello"
print(hi())  # HELLO

"""
📌 데코레이터에서 인수 받기
--------------------------------
    def deco(func):
        def wrapper(x):
            return func(x).upper()
        return wrapper


📌 여러 개 데코레이터 가능
--------------------------------
@A
@B
def f():
    ...

→ 실행 순서: B → A


📌 functools.wraps 로 메타데이터 보존
--------------------------------
    @functools.wraps(func)
"""
# 파이썬 데코레이터 인수 받기 및 메타데이터 보존

import functools
def deco(func):
    @functools.wraps(func)
    def wrapper(x):
        return func(x).upper()
    return wrapper
@deco
def greet(name):
    return f"Hello, {name}"
print(greet("Alice"))  # HELLO, ALICE
print(greet.__name__)  # greet
# 결과 : greet

"""
============================================================
📌 22. Lambda (익명 함수)
============================================================

형식:
    lambda 인수 : 표현식

예:
    x = lambda a: a+10

익명, 한 줄, 표현식만 가능


📌 map / filter / sorted 와 함께 자주 사용
--------------------------------------------
map:
    list(map(lambda x: x*2, nums))

filter:
    list(filter(lambda x: x%2==0, nums))

sorted:
    sorted(items, key=lambda x: x[1])
"""
# 파이썬 Lambda (익명 함수)

x = lambda a: a + 10
print(x(5))  # 15
# 결과 : 15

"""
============================================================
📌 23. 재귀(Recursion)
============================================================
- 함수가 자기 자신을 호출
- 반드시 “기본 케이스”가 필요

예:
    def factorial(n):
        if n <= 1:
            return 1
        return n * factorial(n-1)


📌 피보나치
--------------------------------
    def fib(n):
        if n <= 1:
            return n
        return fib(n-1) + fib(n-2)


📌 재귀 깊이 제한
--------------------------------
    import sys
    sys.getrecursionlimit()
"""
# 파이썬 재귀(Recursion)

def factorial(n):
    if n <= 1:
        return 1
    return n * factorial(n - 1)
print(factorial(5))
# 결과 : 120

def fib(n):
    if n <= 1:
        return n
    return fib(n - 1) + fib(n - 2)
print(fib(6))
# 결과 : 8

import sys
print(sys.getrecursionlimit())  # 기본 재귀 깊이 제한 출력
# 결과 : 3000 (기본값, 시스템마다 다를 수 있음)

"""
============================================================
📌 24. 제너레이터 (Generator)
============================================================

● yield 사용
● 실행을 일시중지/재개
● 메모리 절약

예:
    def gen():
        yield 1
        yield 2


📌 next()로 수동 호출
--------------------------------
    g = gen()
    next(g)


📌 제너레이터 표현식
--------------------------------
    (x*x for x in range(5))


📌 무한 피보나치 생성기
--------------------------------
    def fib():
        a,b = 0,1
        while True:
            yield a
            a,b = b, a+b
"""
# 파이썬 제너레이터 (Generator)

def gen():
    yield 1
    yield 2
g = gen()
print(next(g))
print(next(g))
# 결과 : 1 2

gen_exp = (x * x for x in range(5))
for val in gen_exp:
    print(val)  
# 결과 : 0 1 4 9 16

def fib():
    a, b = 0, 1
    while True:
        yield a
        a, b = b, a + b
fibonacci = fib()
for _ in range(6):
    print(next(fibonacci))
# 결과 : 0 1 1 2 3 5

