"""
📌 변수 (Variables)
====================

변수란?
--------
변수는 데이터를 저장하는 컨테이너(Container)입니다.
"""

"""
📌변수 생성
-----------
Python에서는 변수를 선언하는 명령이 없습니다.
변수는 **처음 값을 할당하는 순간 자동으로 생성**됩니다.

예:
    x = 5
    y = "John"
    print(x)
    print(y)
"""
# 파이썬 변수 생성

x = 5
y = "John"
print(x)
print(y)
# 결과
# 5
# John

"""
📌 변수는 타입 선언이 필요 없음
------------------------------
Python 변수는 미리 데이터 유형(type)을 지정할 필요가 없으며,
값을 다시 할당하면 **유형이 변경될 수 있습니다.**

예:
    x = 4       # x는 int
    x = "Sally" # x는 str로 타입 변경
    print(x)
"""
# 파이썬 변수 타입 선언

x = 4       # x는 int
x = "Sally" # x는 str로 타입 변경
print(x)
# 결과 : Sally

"""
📌 캐스팅(Casting)
-----------------
필요하다면 원하는 데이터 유형으로 캐스팅할 수 있습니다.

예:
    x = str(3)    # 결과: '3'
    y = int(3)    # 결과: 3
    z = float(3)  # 결과: 3.0
"""
# 파이썬 캐스팅

x = str(3)    # 결과: '3'
y = int(3)    # 결과: 3
z = float(3)  # 결과: 3.0
print(x)
print(y)
print(z)
# 결과 : 
# 3
# 3
# 3.0

"""
📌 데이터 유형 확인
------------------
type() 함수를 사용하여 변수의 데이터 유형을 확인할 수 있습니다.

예:
    x = 5
    y = "John"
    print(type(x))
    print(type(y))
"""
# 파이썬 데이터 유형 확인

x = 5
y = "John"
print(type(x))
print(type(y))
# 결과 : 
# <class 'int'>
# <class 'str'>

"""
📌 문자열 따옴표 (', ")
---------------------
문자열은 작은따옴표(' ') 또는 큰따옴표(" ") 모두 사용할 수 있습니다.

예:
    x = "John"
    # same as
    x = 'John'
"""
# 파이썬 문자열 따옴표

x = "John"
print(x) # 결과 : John
# same as
x = 'John'
print(x) # 결과 : John

"""
📌 대소문자 구분 (Case-Sensitive)
-------------------------------
Python 변수 이름은 **대소문자를 구분**합니다.
따라서 아래 두 변수는 서로 다른 변수입니다.

예:
    a = 4
    A = "Sally"
    # A는 a를 덮어쓰지 않습니다.
"""
# 파이썬 대소문자 구분

a = 4
A = "Sally"
# A는 a를 덮어쓰지 않습니다.
print(a)
print(A)
# 결과 :
# 4
# Sally
