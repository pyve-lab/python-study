"""
==========================================================
📌 파이썬 파일 삭제 (Delete File)
==========================================================

파일을 삭제하려면 OS 모듈을 사용해야 한다.

    import os

os.remove("파일명") → 파일 삭제
"""

"""
==========================================================
📌 1. 파일 삭제 기본 사용
==========================================================

import os
os.remove("demofile.txt")

✔ 지정한 파일을 삭제  
✔ 파일이 없으면 오류 발생
"""
# 파이썬 파일 삭제 예시

import os
# os.remove("demofile.txt")  # demofile.txt 파일 삭제

# f = open("demofile.txt", "r")  # 오류 발생: FileNotFoundError: [Errno 2] No such file or directory: 'demofile.txt'

"""
==========================================================
📌 2. 삭제 전 파일 존재 여부 확인하기
==========================================================

삭제 시 파일이 없으면 오류가 발생할 수 있으므로  
os.path.exists() 로 존재 여부를 먼저 확인하는 것이 안전하다.

import os

if os.path.exists("demofile.txt"):
    os.remove("demofile.txt")
else:
    print("The file does not exist")

✔ 파일이 존재하면 삭제  
✔ 존재하지 않으면 안내 메시지 출력  
✔ 안전한 파일 삭제 패턴
"""
# 파이썬 파일 삭제 전 존재 여부 확인 예시

import os
if os.path.exists("demofile.txt"):
    os.remove("demofile.txt")
    print("File deleted successfully.")
else:
    print("The file does not exist")

# 결과 :
# The file does not exist

"""
==========================================================
📌 3. 폴더(디렉터리) 삭제 — os.rmdir()
==========================================================

폴더 전체를 삭제하려면 os.rmdir() 를 사용한다.

import os
os.rmdir("myfolder")

✔ 단, 폴더가 **비어 있어야만** 삭제 가능  
✔ 파일이나 다른 폴더가 들어 있으면 오류 발생
"""
# 파이썬 폴더 삭제 예시

import os
os.rmdir("myfolder")  
# 오류 발생 (test.txt가 들어있는 경우) - OSError: [WinError 145] 디렉터리가 비어 있지 않습니다: 'myfolder'

"""
==========================================================
📌 정리
----------------------------------------------------------
- os.remove("file") → 파일 삭제
- os.path.exists() → 파일 존재 여부 확인
- os.rmdir("folder") → 비어 있는 폴더만 삭제 가능
- 비어 있지 않은 폴더를 삭제하려면 shutil.rmtree() 사용해야 하지만,
  shutil.rmtree()는 매우 위험하므로 신중히 사용해야 함

"""
