"""
==========================================================
📌 파이썬 파일 쓰기 (Write) & 추가(Append) & 파일 생성(Create)
==========================================================

파일에 내용을 기록하기 위해서는 open() 함수의 모드를 지정해야 한다.

✔ "a" → 추가 모드 (append)
✔ "w" → 쓰기 모드 (write, 기존 내용 덮어씀)
✔ "x" → 새 파일 생성 (이미 존재하면 오류)
✔ "a", "w" 는 파일이 없으면 자동으로 새 파일을 생성함
"""

"""
==========================================================
📌 1. 기존 파일에 내용 추가하기 ("a")
==========================================================

with open("demofile.txt", "a") as f:
    f.write("Now the file has more content!")

# 파일 읽기
with open("demofile.txt") as f:
    print(f.read())

✔ "a" 모드는 기존 내용 끝에 새로운 내용을 추가  
✔ 파일이 없으면 자동 생성됨
"""
# 파이썬 파일 추가 모드 ("a") 예시

with open("demofile.txt", "a") as f:
    f.write("Now the file has more content!")

# 파일 읽기
with open("demofile.txt") as f:
    print(f.read())

# 결과 :
# Hello! Welcome to demofile.txt
# This file is for testing purposes.
# Good Luck!Now the file has more content!

"""
==========================================================
📌 2. 기존 파일 내용을 덮어쓰기 ("w")
==========================================================

with open("demofile.txt", "w") as f:
    f.write("Woops! I have deleted the content!")

# 파일 확인
with open("demofile.txt") as f:
    print(f.read())

✔ "w" 모드는 파일 전체 내용을 완전히 덮어쓴다  
✔ 파일이 없으면 새 파일 생성  
✔ 기존 내용은 모두 사라짐 (주의!)
"""
# 파이썬 파일 쓰기 모드 ("w") 예시

with open("demofile.txt", "w") as f:
    f.write("Woops! I have deleted the content!")

# 파일 확인
with open("demofile.txt") as f:
    print(f.read())

# 결과 :
# Woops! I have deleted the content!

"""
==========================================================
📌 3. 새 파일 생성하기 ("x", "a", "w")
==========================================================

파이썬에서 새 파일을 만드는 방법:

✔ "x" → 새 파일 생성, 이미 존재하면 오류 발생  
✔ "a" → 없는 파일이면 새로 생성  
✔ "w" → 없는 파일이면 새로 생성

예:

f = open("myfile.txt", "x")

→ myfile.txt 파일이 존재하지 않으면 생성됨  
→ 이미 존재하면 오류 발생
"""
# 파이썬 새 파일 생성 모드 ("x") 예시

try:
    f = open("myfile.txt", "x")  # 새 파일 생성
    print("File created successfully.")
except FileExistsError:
    print("File already exists.")
    f.close()

f = open("anotherfile.txt", "w")  # 새 파일 생성 (없으면)
f.write("This is another new file.")
f.close()

# 결과 :
# File created successfully.
# 한 번 더 실행 시 :
# File already exists.

"""
==========================================================
📌 정리
----------------------------------------------------------
- "a" : 파일 끝에 내용 추가 (append). 없으면 생성.
- "w" : 파일 덮어쓰기(write). 없으면 생성. 기존 내용 삭제됨.
- "x" : 파일 생성(create). 이미 존재하면 오류 발생.
- 파일 쓰기 후에는 항상 닫거나(with 사용 시 자동) 커서 이동을 고려해야 함.

"""
